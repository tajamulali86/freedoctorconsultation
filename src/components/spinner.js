import React from 'react';
import '../app/Spinner.css' ;
// ng you save the CSS in a separate file called "Spinner.css"

const Spinner = () => {
  return (
    <div className="spinner"></div>
  );
};

export default Spinner;
